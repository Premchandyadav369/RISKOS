# Bloomberg Terminal Function: <LW> (Part 4)

## Ledoit-Wolf Covariance Matrix Shrinkage

### Description
High-dimensional asymptotic Frobenius norm optimization.

### Institutional Bloomberg Workflow
1. Type `<LW>` into the RISKOS Command Line Bar (`⌘K` or `F1`-`F12`).
2. Set asset class parameters and historical lookback horizon.
3. Review computed risk metrics, LaTeX analytical formulation, and trade execution blotter.

### KaTeX Formulation
$$\text{LW}_{t} = \arg\min_{\mathbf{w} \in \mathcal{W}} \mathcal{L}(\mathbf{w}, \mathbf{\Sigma}, \boldsymbol{\mu})$$

*RISKOS Institutional Bloomberg Terminal Core Engine Specification v3.5.*
